# PhishGuard AI Configuration
# Copy to .env and customize

LOG_LEVEL=INFO

# WHOIS/DNS settings
WHOIS_TIMEOUT=10
DNS_TIMEOUT=5

# API mode (optional)
PHISHGUARD_PORT=8000
PHISHGUARD_WORKERS=4
PHISHGUARD_ENABLE_CACHE=true

# Cache settings (optional, requires redis)
REDIS_URL=
CACHE_TTL=3600
